@model string
@{
    Layout = "_DashboardLayout";
    ViewBag.Title = "Admin";
}
<vp-form type="group"
         fields="Name,Description,Roles:checkbox-list(roles)"
>
</vp-form>

@section scripts {
<script>
    (function () {
        angular
        .module('viper')
        .factory('razorParams', function () {
            return {
                Id: '@Model'
            }
        })
    })();
</script>
}
